# BDD v2 contracts

BDD v2 describes a bounded task: goal, repository, owner branch, permitted paths, permissions, verification, state transitions, and evidence. Contracts are validated locally before execution. A contract is a control record, not approval to run a model; a human still creates a budget and reviews results.
