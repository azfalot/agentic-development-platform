# Project Agents Configuration

This repository inherits the **Personal Agentic Development Platform v1**.
Global development policies and core skills are loaded from `~/.agents/`.

## Project Context
- **Project Name:** `<project-name>`
- **Stack:** `<e.g. Node 20 / TypeScript / Express>`
- **Database:** `<project-name>` (on `shared-postgres:5432` / `localhost:5432`)
- **Docker Network:** `dev-network` (external: true)
- **Bounded Contexts:** `<e.g. auth, billing, core>`
- **Project-Specific Exceptions:** `None`
