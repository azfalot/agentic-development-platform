# Legacy configuration

This public repository no longer writes global agent configuration or synchronizes third-party tools automatically. Use `scripts/bootstrap.ps1` with an explicit `AGENT_PLATFORM_HOME`; it refuses to overwrite an existing installation.
